[project]
name = "opentelemetry-samples-python"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "ruff==0.15.15",
]

[tool.uv]
index-strategy = "first-index"

[[tool.uv.index]]
url = "https://pypi.org/simple"
default = true
