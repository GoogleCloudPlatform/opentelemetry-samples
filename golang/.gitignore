# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built by `go test -c`
*.test

# Output of the go coverage tool, specifically `go test -coverprofile`
*.out

# Specific binary for the sample
metric/otlpgrpc/otlpgrpc
